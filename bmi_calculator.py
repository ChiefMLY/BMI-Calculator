#Body mass index (BMI) calculator
#Prompts the user to enter their height(m) and weight(kg)
height = input("enter your height in m: ")
weight = input("enter your weight in kg: ")

#Calculates the bmi using the standard bmi formula and prints the ans as an integar
bmi = float(weight) / float(height) ** 2)

#Converting the bmi to an integar
bmi_int = int(bmi)

#Print the bmi
print(bmi)
